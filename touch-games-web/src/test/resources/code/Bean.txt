/*
 * Copyright (c) 1999-2009 Touch Tecnologia e Informatica Ltda. Gomes de
 * Carvalho, 1666, 3o. Andar, Vila Olimpia, Sao Paulo, SP, Brasil. Todos os
 * direitos reservados. Este software e confidencial e de propriedade da Touch
 * Tecnologia e Informatica Ltda. (Informacao Confidencial). As informacoes
 * contidas neste arquivo nao podem ser publicadas, e seu uso esta limitado de
 * acordo com os termos do contrato de licenca.
 */

package br.com.touchtec.dali.showcase.crud.search;


import java.util.Date;

import br.com.touchtec.dali.crud.api.AssociationDTO;
import br.com.touchtec.dali.crud.api.CrudDTO;
import br.com.touchtec.dali.crud.config.CrudMapping;
import br.com.touchtec.dali.crud.search.CustomClauseBuilder;
import br.com.touchtec.dali.crud.search.CustomQueryFilter;
import br.com.touchtec.dali.crud.search.OrderBy;
import br.com.touchtec.dali.crud.search.SearchClause;
import br.com.touchtec.dali.view.View;
import br.com.touchtec.message.Named;
import br.com.touchtec.twf.SearchMode;

/**
 * Foo
 */
@Foo("bar")
/**
 * Bar
 */
@Bar
/**
 * Comentário da classe.
 */
public class Bean {

    private static final long serialVersionUID = 1L;

    private Long id;

    /**
     * Atributo name
     */
    private String name;

    // Para buscar países por vários names de uma só vez (seperados por espaço)
    // por ex: Brazil Argentina
    private String multipleNames;


    public Long getId() { return this.id; }

    /**
     * Método setName
     */
    public void setId(Long id) { this.id = id; }

    /**
     * Annotation1
     */
    @Annotation1("foo")
    /**
     * Método getName
     */
    public String getName() { return this.name; }

    public void setName(String name) { this.name = name; }

    @Annotation2("foo2")
    @Annotation3("foo3")
    /**
     * Método getMultipleNames
     */   
    public String getMultipleNames() { return this.multipleNames; }

    @Annotation4("foo4")
    public void setMultipleNames(String multipleNames) {this.multipleNames = multipleNames; }
}
